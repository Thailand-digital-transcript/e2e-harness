services/*/app.jar
target/
*.class

# Synced from ../transcript-approval-ui by scripts/sync-realm.sh
infra/keycloak/realm-export.json

# Local-only working files (workspace convention — never committed)
CLAUDE.md
.claude/
docs/superpowers/
sdd/

# IDE
.idea/
.vscode/
*.iml
*.iws
*.ipr
.classpath
.project
.factorypath
.settings/

# OS
.DS_Store
.DS_Store?
._*
*.log
logs/
.worktrees

# Convenience symlink to ../../etax (build-jars.sh resolves the CSC service there).
# No trailing slash: it is a symlink, which git treats as a file, so `etax/` never matched.
etax